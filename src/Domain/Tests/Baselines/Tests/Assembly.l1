fn WriteLine(text: char*, length: lengthof text): void
{
    assembly:
        target: (Target::Os.Darwin)
    {
        Store32 R0, 0x02000004; // rax
        Store32 R7, 1;          // rdi
        Store64 R6, $text;      // rsi
        Store32 R3, $length;    // rdx
        End;
    }
}


fn OnProcessStart(): void
{
    WriteLine("Some");
}
