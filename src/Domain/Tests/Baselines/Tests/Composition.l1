


class Human:
    context (

    )
{
    _text: char*;
    _length: usize;

    String(text: char* literal);
    Length(): usize;
    ...Behaviour;
}


class Behavior from Human
{
    BeAngry(): void;
    BeGlad(): void;
}